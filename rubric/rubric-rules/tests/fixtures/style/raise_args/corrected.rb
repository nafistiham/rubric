# exploded style — these are all correctly written (no violation)
raise RuntimeError, "something went wrong"
raise ArgumentError, "bad argument"
raise TypeError, "wrong type"
raise SomeError.new
raise SomeError.new()
raise
raise "plain string message"
